<div id="main">
	<h2>Adding metadata to page: <%= @page.name %></h2>

	<% form_for :metatag, :url => {:action => ""}  do |f| %>	
	  <%= render :partial => "form", :locals => {:f =>f}%>
	  <p>
	    <%= f.submit 'Create' %>
	  </p>
	<% end %>

	<%= link_to 'Back', admin_metatag_index_path %>
</div>