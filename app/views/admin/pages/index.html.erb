<div id="side-nav">
	<ul>
		<%= link_to "Back", :back %>
	</ul>
</div>

	<h1><%= t('pages.index.title')%> <b><%= " :"+@parent.name if params[:parent]%></b></h1>

	<div id="itsthetable">	
		<table class="page_list">
			<thead>
				<th><%= t('headerlist.name')%></th>	
				<th><%= t('headerlist.title')%></th>
				<th><%= t('headerlist.options')%></th>
				<th>Sublevel</th>	
			</thead>			
			<%= render :partial => "page", :collection => @pages %>
		</table>
	</div>

